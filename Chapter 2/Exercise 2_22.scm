#lang planet neil/sicp
;SICP 2.22

; First version doesn't work, the answer is built up starting w/ first element, which is front-appended.
; Second version has leaves in the right order, but isn't really a list as the car is always a list.

